Description:
    This generator will install all the necessary configuration files for the devies_token_auth gem

Example:
    rails generate devise_token_auth config

    This will create:
        config/initializers/devise_token_auth.rb
